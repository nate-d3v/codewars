//Sort arrays - 1
// input: names - unsorted array
// output: sorted array
sortme = function (names) {
	return names.sort();
};

//Gauß needs help! (Sums of a lot of numbers).
function f(n) {
	return Number.isInteger(n) && n > 1 ? (n * (n - 1)) / 2 + n : false;
}
