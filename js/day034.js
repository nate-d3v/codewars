//Volume of a Cuboid
class Kata {
	static getVolumeOfCuboid(length, width, height) {
		return length * width * height;
	}
}

//Sum Mixed Array
function sumMix(x) {
	return x.reduce((t, n) => t + +n, 0);
}
